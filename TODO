change the text colour 5
add the template folder 4

extras
   -replace exits with crosses and highlight red
   -replace delete with trash cans and have the confirm box tell what it does
   -replace logout with symbol
   -replace share with symbol
   -replace load icon with file folder
   -add a little subtitle which says how many memes each account has when sharing

   -auto colour bars
   
